# TODO / Next Steps

Prioritized under the v2 lens ([00 — premises P-A/P-B, rubric R7](research/00-methodology-and-rubrics.md)): items must pass founder-fit gates (dormancy tolerance, energy return, affordable loss) before opportunity scores matter. Out-of-scope and currently intractable items are kept honestly in the **Icebox** with explicit revisit triggers instead of being silently dropped.

## Owner actions (the bot cannot do these — everything below eventually gates on them)

| # | Action | Unblocks |
|---|--------|----------|
| O1 | **Open/merge the PR chain**: [`issue-2-target-measure`](https://github.com/hzmchen/KONjecture-alpha/tree/issue-2-target-measure) → [`now-items-implementation`](https://github.com/hzmchen/KONjecture-alpha/pull/new/now-items-implementation) (stacked; PAT can push branches but not open PRs) | everything landing on `main`; CI on main |
| O2 | **Close [Issue #2](https://github.com/hzmchen/KONjecture-alpha/issues/2)** referencing [research/09](research/09-target-measure-decision.md) (option 4 confirmed 2026-07-06; bot cannot comment on issues) | bookkeeping |
| O3 | **Create a free ALFRED API key** (alfred.stlouisfed.org) and provide it (e.g. `ALFRED_API_KEY` env var / repo secret) | US outcome vintages beyond `advance` → `fixed_h<k>` target rule → D2 → the scientific leaderboard default |
| O4 | ~~**Sign off D1 + D2**~~ **done 2026-07-06** ([09 §5](research/09-target-measure-decision.md)): D1 = advance / preliminary flash (nowcast concept; RTD proxy disclosed); D2 = `fixed_h8` (k=12 sensitivity); plus new requirement: **scores stratified by horizon bucket** ([schema](docs/schema.md)) — nowcast vs long-term never pooled | X2 unblocked (first-release scoring now; fixed-horizon US after O3) |
| O5 | **Choose the repo license** (constraint from [components §5](research/components/README.md): GPL-3 estimation cores ⇒ GPL-compatible; MIT qualifies) and add `LICENSE`; also fills the `license` field in [.zenodo.json](.zenodo.json) | first public release; Zenodo escrow metadata |
| O6 | **Create a Zenodo account/token** for DOI escrow snapshots ([RELEASING.md](RELEASING.md)) | X9 escrow step |
| O7 | **Install the CI workflow**: `git mv ci/tests.yml .github/workflows/tests.yml && git commit && git push` (bot PAT lacks the `workflow` scope to push workflow files) | fail-loud CI (X9) |

## Now (first concrete steps)

| # | Item | Why now | Notes |
|---|------|---------|-------|
| N1 | ~~**Target measure**~~ **decided 2026-07-06**: [Issue #2](https://github.com/hzmchen/KONjecture-alpha/issues/2) option 4 confirmed by owner — store outcome-as-of-vintage; headline = first publication, scientific = fixed-horizon ([09](research/09-target-measure-decision.md)). Residual sub-decisions live where they gate: D1+D2 → X2 trigger, D3 → N4 schema, D4 → X5 | schema rule fixed for N2/N4: every archived outcome row carries its publication vintage; scores are (forecast, target-rule) pairs | Issue #2 can be closed (owner action; bot cannot comment) |
| N2 | ~~**Riskiest-assumption spike**~~ **passed 2026-07-06** → [pipeline/](pipeline/README.md): `targets` pipeline (FRED pull cached → AR(2) benchmark + `dfms` DFM bridge → point + 7 quantiles hubverse-style → append-only parquet via `nanoparquet`); one reproducible end-to-end run, re-run skips all targets. **Residuals:** latest-vintage inputs only (ALFRED for vintage-correct), σ-based intervals are placeholders until X2 backtests | tests the *technical* riskiest assumption (solo data plumbing), which the market research could not | success = one reproducible end-to-end run ✓ |
| N3 | ~~**Wizard-of-Oz comparison page**~~ **done 2026-07-06** → [site/](site/): static self-contained page (generated by `site/build_site.py` from the archive) — 2026Q2 evolution, 12-quarter track record vs BEA advance, common-sample MAE, ECB SPF section; implements 08-F3 staleness banner + 09-D4 target disclosure. Residual: value-hypothesis *validation* (does anyone find it useful?) is X7-adjacent, not a build task | tests the core J2 value hypothesis in days; zero standing infra | data per [06 §3.1](research/06-vintage-reconstruction.md) |
| N4 | ~~**Seed the archive retroactively**~~ **substantially done 2026-07-06** → [ingest/](ingest/README.md), [data/archive/](docs/schema.md): 3,285 forecasts (GDPNow daily 2011→ incl. live quarter, NY Fed 2022→, Philly SPF 1968→, ECB SPF 1999→) + 59 advance-estimate outcomes with release dates. **Residuals:** ALFRED outcome vintages (needs free API key → unlocks fixed-horizon rule/D2), EA outcomes (Eurostat flash), NY Fed 2016–21 legacy file | removes cold start; the cheapest most differentiating win; first two tracks also surface the data needed to fix D1+D2 | [06 §3](research/06-vintage-reconstruction.md) |
| N5 | ~~**webR/WASM package audit** for the intended view-layer stack~~ **done** → [components/06 §3](research/components/06-view-layer-delivery.md): 31/32 stack packages have WASM binaries (incl. C++-based `dfms`); only `arrow` missing, substitutable by `nanoparquet`/`duckdb` — **shinylive default confirmed** | decided shinylive default ([05 §3](research/05-shiny-risks-and-alternatives.md)) | residual: 1-h in-browser smoke test of the app skeleton, folded into N3/X5 |

## Next (after the spike proves out)

| # | Item | Trigger |
|---|------|---------|
| X1 | ~~Review mode (expected vs realised) over the seeded archive~~ **done 2026-07-06**: error chart (final nowcast − first print, lollipop vs zero axis) as a Review section on the page and a Review tab on the dashboard | N4 done ✓ |
| X2 | ~~`scoringutils`-based scoring + first vintage-correct backtest~~ **done 2026-07-06** → [konjecture/R/scoring.R](konjecture/R/scoring.R), `data/archive/scores{,_summary}.*`: 3,099 forecasts scored vs `first_release` (US advance + EA yoy via RTD), **stratified by horizon bucket** (owner requirement — nowcast vs long-term never pooled); surfaced on page + dashboard. First results: GDPNow backcast MAE 0.92 vs nowcast 1.59 SAAR pp; ECB SPF 1y 0.99 vs 2y 1.59 yoy pp. **EA `fixed_h8` shipped 2026-07-07** (`score_fixed_h`, konjecture pkg): settled scores vs the vintage 8 quarters after quarter end (k=12 sensitivity in the score files, not the landing view — 09 §4); page + dashboard show Target column per D4 (first print always, settled fills in, unsettled blank). **Residual:** US fixed-horizon after O3/ALFRED | D1+D2 decided ✓ ([09 §5](research/09-target-measure-decision.md)), N4 done ✓ |
| X3 | ~~Add 2nd/3rd model class (`midasr`) behind a thin model-adapter interface~~ **done 2026-07-08** → [konjecture/R/models.R](konjecture/R/models.R): adapter contract `adapter(data, target_q) -> list(model_id, target, point, q)` with `validate_forecast` enforcement; `model_adapters()` registry wraps AR(2) + DFM bridge and adds `konjecture_midas_bridge` (U-MIDAS via `midasr::midas_r`, `nealmon` lag polynomial over 9 monthly INDPRO lags, partial target quarter mean-padded — disclosed) whenever `midasr` is installed; pipeline runs the whole registry via `run_adapters` ([_targets.R](pipeline/_targets.R)). First joint nowcast 2026Q2 (SAAR %): AR2 2.82 / DFM 3.25 / MIDAS 3.86. Also fixed: cached-`Sys.Date()` `run_date` (now `cue = always`; stale forecast_date would break vintage correctness) and `pipeline/.Rprofile` so `tar_make()` sees the renv library. **Residual:** `nowcast_lstm` via reticulate stays out until an R-native need justifies a Python toolchain | N2 architecture stable ✓ |
| X4 | Expand regions: US, EA, DE, FR, IT, ES, UK | ingestion adapters generalized |
| X5 | Shinylive (or Quarto fallback) dashboard with Now/Compare modes. **Quarto fallback shipped 2026-07-06** → [site/dashboard.qmd](site/dashboard.qmd) (value boxes, Now/track-record/table/EA tabs, staleness banner; reuses tested `site_lib.R` charts; `dashboard_html` target in the pipeline, soft-skipped without the quarto CLI). **Shinylive skeleton shipped + smoke-tested 2026-07-07** → [site/app/](site/app/README.md): in-browser WASM app (Now/Compare/Scores over precomputed artifacts), boots ~21 s headless, all tabs verified (N5 residual discharged). **Dual-target presentation done for EA** (D4 Target column on page/dashboard/app, k=12 in files only); US settled column needs ALFRED (O3). Staleness requirement ([08 F3](research/08-pre-mortem.md)) implemented on all three views. Remaining: deployment (owner call, after O1+O5) | N3 validated the view; N5 audited |
| X6 | ~~**Methodology debt:** sensitivity check of rubric scores (±1 perturbation)~~ **done 2026-07-07** → [10](research/10-rubric-sensitivity.md): no single ±1 perturbation crosses an action bucket; G1-as-MVP robust; G2/G3 and G4/G5 are unordered tiers (never rely on their strict order). ~~Negative-case sweep + pre-mortem~~ **done** → [07](research/07-graveyard-negative-cases.md), [08](research/08-pre-mortem.md) | before any public "launch" framing |
| X7 | Optional: 3–5 primary conversations (ESCoE, hubverse community, r/econometrics) | when external feedback would be fun, not before |
| X8 | ~~IMF WEO vintage ingestion (global annual institutional track)~~ **done 2026-07-08** → [konjecture/R/weo.R](konjecture/R/weo.R) + `weo_*` entries in [download_raw.R](ingest/download_raw.R): 15 WEO editions (Apr 2018 → Apr 2025) as `imf_weo` forecast rounds — per edition, `NGDP_RPCH` years beyond its own `Estimates Start After` marker become annual forecast rows (86 US rows; all countries kept in the trimmed cache for X4). Survived three IMF layout eras: all-lowercase CDN paths, cp1252 legacy files, BOM-less UTF-16LE from 2024 on; the trim hook validates payloads so 200-with-error-page CDN responses fall through the candidate-URL chain. **Residuals:** (a) Oct 2025 + Apr 2026 editions moved to the new data.imf.org portal (bulk file no longer on the edition page; IMF's `WEOhistorical.xlsx` is the documented fallback), (b) **OECD EO not feasible keyless**: archived EO vintages went away with stats.oecd.org's retirement — sdmx.oecd.org exposes the current edition only, (c) annual-target scoring rule (WEO rows are archived but unscored: scoring is quarterly-target only) | X1 shipped ✓ |
| X10 | **Extract core functionality into an R package** per the ground rules in [CLAUDE.md](CLAUDE.md): `pipeline/R/` (functions, scoring), `site/R/` chart/site lib, and shared logic inside `ingest/` scripts become package code (standard `R/` + `tests/testthat` layout, no hard-coded paths); `_targets.R`, `ingest/`, `site/` scripts become thin usage layers. Incremental — migrate whatever a task touches, don't big-bang | ground rules adopted 2026-07-07; applies from the next code-touching task on |
| X9 | ~~**Pre-mortem countermeasures**~~ **substantially done 2026-07-06** ([08 §4](research/08-pre-mortem.md)): fail-loud CI ([ci/tests.yml](ci/tests.yml) — offline suite on push + monthly heartbeat; **owner must `git mv ci/tests.yml .github/workflows/` (O7)**, the bot's PAT lacks `workflow` scope) ✓ · dependency register with fallbacks + license registry ([ingest/README](ingest/README.md)) ✓ · no-SLA note ✓ · no-streak rule ([RELEASING.md](RELEASING.md)) ✓ · SUNSET skeleton ([SUNSET.md](SUNSET.md)) ✓ · Zenodo escrow procedure + [.zenodo.json](.zenodo.json) ready — needs O5 (license) + O6 (Zenodo token) | with first public artifact (N3/X5) ✓ |

## Later (valuable, not yet)

| # | Item | Why not now |
|---|------|------------|
| L1 | Public leaderboard with uncertainty-aware scores (WIS/CRPS) | needs X2 maturity + fairness rules (per-track declared targets, no cross-region ranking, pre-declared default views — [09 §4](research/09-target-measure-decision.md); information sets) |
| L2 | Central-bank projection PDFs → forecasts (AI-assisted parsing, one-off per institution) | high effort per source; validation attention is the bottleneck |
| L3 | API / data export for third parties | creates expectations (obligation audit) — only with clear boundaries ("static files, no SLA") |
| L4 | German-language supply sweep (ifo, IfW, DIW, KOF nowcasts) to close the v1 language gap | cheap but not blocking; fold into X4 |

## Icebox (out of scope or intractable now — with revisit triggers)

| # | Item | Why iceboxed | Revisit when |
|---|------|--------------|--------------|
| I1 | **Paper→model pipeline** ("fetch recent research articles and turn them into forecasting machines", Issue #1) | speculative, R6 score 5/75; enormous validation burden | a concrete paper is hand-ported first (do it manually twice, then judge automation) |
| I2 | **Interactive user-triggered re-runs of forecasts/evaluations** | worst R7 offender: compute in request path, standing infra ([05 RS4](research/05-shiny-risks-and-alternatives.md)) | precomputed scenario grids prove insufficient *and* a server is accepted consciously |
| I3 | **Institutional web scraping at scale** | code is cheap (P-B) but babysitting + legal review are attention sinks | a wanted source has no downloadable archive *and* stable markup |
| I4 | **Alt-data ingestion** (Google Trends, mobility, freight, news) | ToS/licensing review per source; pipelines rot fast (dormancy fail) | one specific alt-data source materially improves a tracked model's backtest |
| I5 | **Sub-national coverage beyond available official series** | data scarcity; research-grade methods (NUTS2 MF-DFM) not yet productizable solo | ESCoE-style official regional series become API-accessible for a target country |
| I6 | **Domain-agnostic forecast-evaluation framework** (vision's end state) | premature abstraction; satisfied "by construction" via hubverse-compatible formats | a second domain (e.g. epi course project) actually tries to reuse the stack |
| I7 | **Consortium/governance structures** | one person; governance is overhead without a community | ≥2 sustained external contributors exist |
| I8 | **Wayback-Machine reconstruction** of unarchived forecast pages | high variance, low yield | a specific high-value gap in the seeded archive justifies it |
| I9 | **User-selectable databases for forecast production** | multiplies cache/compute surface | core single-database flow is boring-stable |

## Standing audits (quarterly, from [03 §6](research/03-synthesis-market-gap-and-fit.md))

- **Fun audit** — which parts energized/drained? Redesign or icebox the drains.
- **Obligation audit** — any standing duties to strangers? Automate to indifference or retire.
- **Affordable-loss audit** — time & compute spend within budget.
